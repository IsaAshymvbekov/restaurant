# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*.swo

# PHP / local config overrides (keep defaults in repo; use local overrides if needed)
# config/db.local.php

# Secrets (never commit real credentials)
.env
.env.*
*.pem

# Optional tooling
vendor/
node_modules/
